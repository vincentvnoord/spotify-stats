
export type BasicTrackInfo = {
    name: string;
    ranking: number;
    artists: { name: string }[];
    image: string;
}

export type BasicArtistInfo = {
    name: string;
    ranking: number;
    image: string;
}